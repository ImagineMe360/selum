name: Selenium UI Test

on:
  pull_request:
    branches: [develop]
  push:
    branches: [main]
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  selenium-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install frontend dependencies
        run: npm install

      - name: Start dev server
        # run: npm run dev &
        if: github.event_name != 'schedule'
        run: npm run dev &
        env:
          CI: true

      - name: Wait for dev server
        if: github.event_name != 'schedule'
        uses: jakejarvis/wait-action@master
        with:
          url: 'http://localhost:5173'
          timeout: '60'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install selenium pytest

      - name: Install Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Install ChromeDriver
        uses: nanasess/setup-chromedriver@v2

      - name: Run Selenium tests
        run: pytest -v
        env:
          # If schedule run → use production URL (Netlify)
          BASE_URL: ${{ github.event_name == 'schedule' && 'https://selum.netlify.app' || 'http://localhost:5173' }}

      - name: Send email on failure
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: app4bells@gmail.com
          password: ilogvjrsdnuxryxc
          subject: '🚨 Selenium Test Failed - ${{ github.repository }}'
          to: app4bells@gmail.com
          from: 'Selenium Watchdog <app4bells@gmail.com>'
          body: |
            One or more Selenium tests have failed.

            Repository: ${{ github.repository }}
            Workflow: ${{ github.workflow }}
            Branch/Trigger: ${{ github.ref_name }} / ${{ github.event_name }}
            Commit: ${{ github.sha }}
            Link: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

      - name: Send email on success
        if: success()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: app4bells@gmail.com
          password: ilogvjrsdnuxryxc
          subject: '✅ Selenium Test Passed - ${{ github.repository }}'
          to: app4bells@gmail.com
          from: 'Selenium Watchdog <app4bells@gmail.com>'
          body: |
            All Selenium tests have passed successfully 🎉

            🏷️ Repository: ${{ github.repository }}
            ⚡ Workflow: ${{ github.workflow }}
            🌿 Branch/Trigger: ${{ github.ref_name }} / ${{ github.event_name }}
            🧾 Commit: ${{ github.sha }}
            🔗 Link: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
